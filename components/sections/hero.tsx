export default function HeroSection() {
  return (
    <section className="relative flex min-h-screen items-center justify-center bg-background px-6">
      <div className="mx-auto max-w-5xl text-center">
        {/* Headline */}
        <h1 className="text-4xl font-bold uppercase tracking-tight text-foreground md:text-6xl lg:text-7xl">
          {/* INSTITUTIONAL LIQUIDITY. AGGREGATED. DELIVERED. */}
        </h1>

        {/* Subtext */}
        <p className="mx-auto mt-6 max-w-2xl text-lg text-muted-foreground">
          {/* Subtext content */}
        </p>

        {/* CTA Button */}
        <div className="mt-10">
          {/* Request Access button */}
        </div>
      </div>
    </section>
  );
}
